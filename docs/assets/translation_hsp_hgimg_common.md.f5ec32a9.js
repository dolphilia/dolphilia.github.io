import{_ as e,o as s,c as p,Q as t}from"./chunks/framework.43c433ff.js";const h=JSON.parse('{"title":"HGIMG4共通","description":"","frontmatter":{},"headers":[],"relativePath":"translation/hsp/hgimg_common.md","filePath":"translation/hsp/hgimg_common.md","lastUpdated":1676126774000}'),n={name:"translation/hsp/hgimg_common.md"},a=t(`<h1 id="hgimg4共通" tabindex="-1">HGIMG4共通 <a class="header-anchor" href="#hgimg4共通" aria-label="Permalink to &quot;HGIMG4共通&quot;">​</a></h1><p><strong>対応</strong>:</p><ul><li>Win</li></ul><p><strong>対応の補足</strong>:</p><p>HGIMG4DXはDirectX9、HGIMG4はOpenGL3.1環境で動作</p><h2 id="fvseti" tabindex="-1">fvseti <a class="header-anchor" href="#fvseti" aria-label="Permalink to &quot;fvseti {#fvseti}&quot;">​</a></h2><p>整数値からベクトル設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入される変数名 (x,y,z) = 整数値</p><p><strong>説明</strong>:</p><p>(x,y,z)で指定された整数値をベクトルとしてFV値に代入する。</p><p><strong>参照</strong>:</p><p>fvset fvadd fvsub fvmul fvdiv fvdir fvmin fvmax fvouter fvinner fvface fvunit</p><h2 id="fvset" tabindex="-1">fvset <a class="header-anchor" href="#fvset" aria-label="Permalink to &quot;fvset {#fvset}&quot;">​</a></h2><p>ベクトル設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入される変数名 (x,y,z) = 計算値(実数値)</p><p><strong>説明</strong>:</p><p>(x,y,z)で指定された小数値(X,Y,Z)をベクトルとしてFV値に代入する。</p><p><strong>参照</strong>:</p><p>fvseti fvadd fvsub fvmul fvdiv fvdir fvmin fvmax fvouter fvinner fvface fvunit</p><h2 id="fvadd" tabindex="-1">fvadd <a class="header-anchor" href="#fvadd" aria-label="Permalink to &quot;fvadd {#fvadd}&quot;">​</a></h2><p>ベクトル加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 計算値(実数値)</p><p><strong>説明</strong>:</p><p>(x,y,z)で指定された小数値(X,Y,Z)をFV値に加算する。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvsub fvmul fvdiv fvmin fvmax</p><h2 id="fvsub" tabindex="-1">fvsub <a class="header-anchor" href="#fvsub" aria-label="Permalink to &quot;fvsub {#fvsub}&quot;">​</a></h2><p>ベクトル減算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 計算値(実数値)</p><p><strong>説明</strong>:</p><p>(x,y,z)で指定された小数値(X,Y,Z)をFV値から減算する。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvadd fvmul fvdiv fvmin fvmax</p><h2 id="fvmul" tabindex="-1">fvmul <a class="header-anchor" href="#fvmul" aria-label="Permalink to &quot;fvmul {#fvmul}&quot;">​</a></h2><p>ベクトル乗算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 計算値(実数値)</p><p><strong>説明</strong>:</p><p>(x,y,z)で指定された小数値(X,Y,Z)をFV値に並列で乗算する。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvadd fvsub fvdiv fvmin fvmax</p><h2 id="fvdiv" tabindex="-1">fvdiv <a class="header-anchor" href="#fvdiv" aria-label="Permalink to &quot;fvdiv {#fvdiv}&quot;">​</a></h2><p>ベクトル除算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 計算値(実数値)</p><p><strong>説明</strong>:</p><p>(x,y,z)で指定された小数値(X,Y,Z)をFV値に並列で除算する。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvadd fvsub fvmul fvmin fvmax</p><h2 id="fvdir" tabindex="-1">fvdir <a class="header-anchor" href="#fvdir" aria-label="Permalink to &quot;fvdir {#fvdir}&quot;">​</a></h2><p>ベクトル回転</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 回転角度(実数値)</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているFV値をX,Y,Z角度として、 小数値(X,Y,Z)で指定されたベクトルを回転させた結果を、変数fvに代入します。</p><p><strong>参照</strong>:</p><p>fvset fvdir fvface</p><h2 id="fvface" tabindex="-1">fvface <a class="header-anchor" href="#fvface" aria-label="Permalink to &quot;fvface {#fvface}&quot;">​</a></h2><p>座標から角度を得る</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = X,Y,Z座標値(実数値)</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているベクトル(FV値)を基点とするX,Y,Z座標から、指定されたX,Y,Z座標を直線で見るためのX,Y,Z回転角度を求めて変数fvに代入します。</p><p><strong>参照</strong>:</p><p>fvset fvdir</p><h2 id="fvmin" tabindex="-1">fvmin <a class="header-anchor" href="#fvmin" aria-label="Permalink to &quot;fvmin {#fvmin}&quot;">​</a></h2><p>ベクトルの要素を最小値で切り詰める</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 比較値(実数値)</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているFV値の各要素を、パラメーターで指定された値(X,Y,Z)が最小値になるように切り詰めます。 FV値の各要素に対して最小値を適用する場合に使用します。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvadd fvsub fvmul fvdiv fvmax</p><h2 id="fvmax" tabindex="-1">fvmax <a class="header-anchor" href="#fvmax" aria-label="Permalink to &quot;fvmax {#fvmax}&quot;">​</a></h2><p>ベクトルの要素を最大値で切り詰める</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 比較値(実数値)</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているFV値の各要素を、パラメーターで指定された値(X,Y,Z)が最大値になるように切り詰めます。 FV値の各要素に対して最大値を適用する場合に使用します。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvadd fvsub fvmul fvdiv fvmin</p><h2 id="fvouter" tabindex="-1">fvouter <a class="header-anchor" href="#fvouter" aria-label="Permalink to &quot;fvouter {#fvouter}&quot;">​</a></h2><p>ベクトル外積</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 演算するベクトル値(実数値)</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているFV値と、小数値(X,Y,Z)で指定するベクトルの外積を求めて代入します。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvinner</p><h2 id="fvinner" tabindex="-1">fvinner <a class="header-anchor" href="#fvinner" aria-label="Permalink to &quot;fvinner {#fvinner}&quot;">​</a></h2><p>ベクトル内積</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,x,y,z fv = FV値が代入されている変数名 (x,y,z) = 演算するベクトル値(実数値)</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているFV値と、小数値(X,Y,Z)で指定するベクトルの内積を求めてfv.0に代入します。</p><p><strong>参照</strong>:</p><p>fvseti fvset fvouter</p><h2 id="fvunit" tabindex="-1">fvunit <a class="header-anchor" href="#fvunit" aria-label="Permalink to &quot;fvunit {#fvunit}&quot;">​</a></h2><p>ベクトル正規化</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv fv = FV値が代入されている変数名</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているベクトル(FV値)を正規化します。</p><p><strong>参照</strong>:</p><p>fvseti fvset</p><h2 id="fsin" tabindex="-1">fsin <a class="header-anchor" href="#fsin" aria-label="Permalink to &quot;fsin {#fsin}&quot;">​</a></h2><p>サインを求める</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fval,frot fval = 実数値が代入される変数名 frot = 回転角度(ラジアン)</p><p><strong>説明</strong>:</p><p>frotで指定された角度のサイン値をfvalで指定した変数に代入します。 角度の単位はラジアン(2π=360度)になります。</p><p><strong>参照</strong>:</p><p>fcos fsqr froti</p><h2 id="fcos" tabindex="-1">fcos <a class="header-anchor" href="#fcos" aria-label="Permalink to &quot;fcos {#fcos}&quot;">​</a></h2><p>コサインを求める</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fval,frot fval = 実数値が代入される変数名 frot = 回転角度(ラジアン)</p><p><strong>説明</strong>:</p><p>frotで指定された角度のコサイン値をfvalで指定した変数に代入します。 角度の単位はラジアン(2π=360度)になります。</p><p><strong>参照</strong>:</p><p>fsin fsqr froti</p><h2 id="fsqr" tabindex="-1">fsqr <a class="header-anchor" href="#fsqr" aria-label="Permalink to &quot;fsqr {#fsqr}&quot;">​</a></h2><p>平方根を求める</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fval,fprm fval = 実数値が代入される変数名 fprm = 演算に使われる値(実数)</p><p><strong>説明</strong>:</p><p>fprmで指定された値の平方根をfvalで指定した変数に代入します。</p><p><strong>参照</strong>:</p><p>fsin fcos froti</p><h2 id="str2fv" tabindex="-1">str2fv <a class="header-anchor" href="#str2fv" aria-label="Permalink to &quot;str2fv {#str2fv}&quot;">​</a></h2><p>文字列をベクトルに変換</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv,&quot;x,y,z&quot; fv = FV値が代入される変数名 &quot;x,y,z&quot; = 「,」で区切られた実数値が格納された文字列</p><p><strong>説明</strong>:</p><p>&quot;x,y,z&quot;で指定された文字列情報を「,」で区切られたX,Y,Z小数値として読み出し、fvで指定された変数に格納します。 それぞれの項目が正しく数値として認識できない(不正な)文字列があった場合には、それ以降の項目も含めて0.0が代入されます。</p><p><strong>参照</strong>:</p><p>fv2str str2f f2str f2i</p><h2 id="fv2str" tabindex="-1">fv2str <a class="header-anchor" href="#fv2str" aria-label="Permalink to &quot;fv2str {#fv2str}&quot;">​</a></h2><p>ベクトルを文字列に変換</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv fv = FV値が代入されている変数名</p><p><strong>説明</strong>:</p><p>fvで指定された変数に格納されているベクトル(FV値)を文字列に変換してシステム変数refstrに結果を返します。</p><p><strong>参照</strong>:</p><p>str2fv str2f f2str f2i</p><h2 id="str2f" tabindex="-1">str2f <a class="header-anchor" href="#str2f" aria-label="Permalink to &quot;str2f {#str2f}&quot;">​</a></h2><p>文字列を小数値に変換</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fval,&quot;fval&quot; fval = 実数値が代入される変数名 &quot;fval&quot; = 実数値が格納された文字列</p><p><strong>説明</strong>:</p><p>&quot;fval&quot;で指定された文字列情報を小数値として読み出し、fvalで指定された変数に格納します。</p><p><strong>参照</strong>:</p><p>fv2str str2fv f2str f2i</p><h2 id="f2str" tabindex="-1">f2str <a class="header-anchor" href="#f2str" aria-label="Permalink to &quot;f2str {#f2str}&quot;">​</a></h2><p>小数値を文字列に変換</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>sval,fval sval = 文字列が代入される変数名 fval = 変換元の実数値</p><p><strong>説明</strong>:</p><p>fvalで指定された小数値を文字列に変換して、valで指定された文字列型の変数に結果を返します。</p><p><strong>参照</strong>:</p><p>fv2str str2fv str2f f2i</p><h2 id="delobj" tabindex="-1">delobj <a class="header-anchor" href="#delobj" aria-label="Permalink to &quot;delobj {#delobj}&quot;">​</a></h2><p>オブジェクトの削除</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>ObjID ObjID : オブジェクトID</p><p><strong>説明</strong>:</p><p>指定されたオブジェクトを削除します。</p><p><strong>参照</strong>:</p><p>regobj</p><h2 id="setpos" tabindex="-1">setpos <a class="header-anchor" href="#setpos" aria-label="Permalink to &quot;setpos {#setpos}&quot;">​</a></h2><p>posグループ情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 設定する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 posグループ(表示座標)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>setang setangr setscale setdir setefx setwork</p><h2 id="setang" tabindex="-1">setang <a class="header-anchor" href="#setang" aria-label="Permalink to &quot;setang {#setang}&quot;">​</a></h2><p>angグループ情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 設定する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 angグループ(表示角度)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。 角度の単位はラジアンになります。 (回転する順番はX-&gt;Y-&gt;Zとなります。他の順番で回転させるための、setangy、setangz命令が用意されています。) 整数で角度を設定するためのsetangr命令も用意されています。</p><p><strong>参照</strong>:</p><p>setpos setangr setscale setdir setefx setwork</p><h2 id="setangr" tabindex="-1">setangr <a class="header-anchor" href="#setangr" aria-label="Permalink to &quot;setangr {#setangr}&quot;">​</a></h2><p>angグループ情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 設定する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 angグループ(表示角度)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。 角度の単位は整数で0〜255で一周する値を使用します。 ラジアンで角度を設定するためのsetang命令も用意されています。</p><p><strong>参照</strong>:</p><p>setpos setang setscale setdir setefx setwork</p><h2 id="setscale" tabindex="-1">setscale <a class="header-anchor" href="#setscale" aria-label="Permalink to &quot;setscale {#setscale}&quot;">​</a></h2><p>scaleグループ情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 設定する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 scaleグループ(表示倍率)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>setpos setang setangr setdir setefx setwork</p><h2 id="setdir" tabindex="-1">setdir <a class="header-anchor" href="#setdir" aria-label="Permalink to &quot;setdir {#setdir}&quot;">​</a></h2><p>dirグループ情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 設定する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 dirグループ(移動ベクトル)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。 移動ベクトルに登録された値は、オブジェクトの自動移動モード(OBJ_MOVE)時に参照されます。</p><p><strong>参照</strong>:</p><p>setpos setang setangr setscale setefx setwork</p><h2 id="setwork" tabindex="-1">setwork <a class="header-anchor" href="#setwork" aria-label="Permalink to &quot;setwork {#setwork}&quot;">​</a></h2><p>workグループ情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 設定する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 workグループ(ワーク値)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>setpos setang setangr setscale setdir setefx setwork2</p><h2 id="addpos" tabindex="-1">addpos <a class="header-anchor" href="#addpos" aria-label="Permalink to &quot;addpos {#addpos}&quot;">​</a></h2><p>posグループ情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 加算する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 posグループ(表示座標)に(x,y,z)で指定された値を設定します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>addang addangr addscale adddir addefx addwork</p><h2 id="addang" tabindex="-1">addang <a class="header-anchor" href="#addang" aria-label="Permalink to &quot;addang {#addang}&quot;">​</a></h2><p>angグループ情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 加算する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 angグループ(表示角度)に(x,y,z)で指定された値を加算します。 (x,y,z)には、実数または整数値を指定することができます。 角度の単位はラジアンになります。 整数で角度を設定するためのsetangr命令も用意されています。</p><p><strong>参照</strong>:</p><p>addpos addangr addscale adddir addefx addwork</p><h2 id="addangr" tabindex="-1">addangr <a class="header-anchor" href="#addangr" aria-label="Permalink to &quot;addangr {#addangr}&quot;">​</a></h2><p>angグループ情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 加算する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 angグループ(表示角度)に(x,y,z)で指定された値を加算します。 (x,y,z)には、実数または整数値を指定することができます。 角度の単位は整数で0〜255で一周する値を使用します。 ラジアンで角度を設定するためのsetang命令も用意されています。</p><p><strong>参照</strong>:</p><p>addpos addang addscale adddir addefx addwork</p><h2 id="addscale" tabindex="-1">addscale <a class="header-anchor" href="#addscale" aria-label="Permalink to &quot;addscale {#addscale}&quot;">​</a></h2><p>scaleグループ情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 加算する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 scaleグループ(表示倍率)に(x,y,z)で指定された値を加算します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>addpos addang addangr adddir addefx addwork</p><h2 id="adddir" tabindex="-1">adddir <a class="header-anchor" href="#adddir" aria-label="Permalink to &quot;adddir {#adddir}&quot;">​</a></h2><p>dirグループ情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 加算する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 dirグループ(移動ベクトル)に(x,y,z)で指定された値を加算します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>addpos addang addangr addscale addefx addwork</p><h2 id="addwork" tabindex="-1">addwork <a class="header-anchor" href="#addwork" aria-label="Permalink to &quot;addwork {#addwork}&quot;">​</a></h2><p>workグループ情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 加算する値 (デフォルト=0)</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを設定します。 workグループ(ワーク値)に(x,y,z)で指定された値を加算します。 (x,y,z)には、実数または整数値を指定することができます。</p><p><strong>参照</strong>:</p><p>addpos addang addangr addscale adddir addefx</p><h2 id="getpos" tabindex="-1">getpos <a class="header-anchor" href="#getpos" aria-label="Permalink to &quot;getpos {#getpos}&quot;">​</a></h2><p>posグループ情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 posグループ(表示座標)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、実数型の変数として設定されます。 命令の最後に「i」を付加することで、整数値として値を取得することができます。</p><p><strong>参照</strong>:</p><p>getposi getang getangr getscale getdir getefx getwork</p><h2 id="getscale" tabindex="-1">getscale <a class="header-anchor" href="#getscale" aria-label="Permalink to &quot;getscale {#getscale}&quot;">​</a></h2><p>scaleグループ情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 scaleグループ(表示倍率)の内容が(x,y,z)で指定された変数に代入されますます。 (x,y,z)は、実数型の変数として設定されます。 命令の最後に「i」を付加することで、整数値として値を取得することができます。</p><p><strong>参照</strong>:</p><p>getscalei getpos getang getangr getdir getefx getwork</p><h2 id="getdir" tabindex="-1">getdir <a class="header-anchor" href="#getdir" aria-label="Permalink to &quot;getdir {#getdir}&quot;">​</a></h2><p>dirグループ情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 dirグループ(移動ベクトル)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、実数型の変数として設定されます。 命令の最後に「i」を付加することで、整数値として値を取得することができます。</p><p><strong>参照</strong>:</p><p>getdiri getpos getang getangr getscale getefx getwork</p><h2 id="getwork" tabindex="-1">getwork <a class="header-anchor" href="#getwork" aria-label="Permalink to &quot;getwork {#getwork}&quot;">​</a></h2><p>workグループ情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 workグループ(ワーク値)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、実数型の変数として設定されます。 命令の最後に「i」を付加することで、整数値として値を取得することができます。</p><p><strong>参照</strong>:</p><p>getworki getpos getang getangr getscale getdir getefx</p><h2 id="getposi" tabindex="-1">getposi <a class="header-anchor" href="#getposi" aria-label="Permalink to &quot;getposi {#getposi}&quot;">​</a></h2><p>posグループ情報を整数で取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 posグループ(表示座標)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、整数型の変数として設定されます。</p><p><strong>参照</strong>:</p><p>getpos getangi getangri getscalei getdiri getefxi getworki</p><h2 id="getscalei" tabindex="-1">getscalei <a class="header-anchor" href="#getscalei" aria-label="Permalink to &quot;getscalei {#getscalei}&quot;">​</a></h2><p>scaleグループ情報を整数で取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 scaleグループ(表示倍率)の内容が(x,y,z)で指定された変数に代入されますます。 (x,y,z)は、整数型の変数として設定されます。</p><p><strong>参照</strong>:</p><p>getscale getposi getangi getangri getdiri getefxi getworki</p><h2 id="getdiri" tabindex="-1">getdiri <a class="header-anchor" href="#getdiri" aria-label="Permalink to &quot;getdiri {#getdiri}&quot;">​</a></h2><p>dirグループ情報を整数で取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 dirグループ(移動ベクトル)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、整数型の変数として設定されます。</p><p><strong>参照</strong>:</p><p>getdir getposi getangi getangri getscalei getefxi getworki</p><h2 id="getworki" tabindex="-1">getworki <a class="header-anchor" href="#getworki" aria-label="Permalink to &quot;getworki {#getworki}&quot;">​</a></h2><p>workグループ情報を整数で取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 workグループ(ワーク値)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、整数型の変数として設定されます。</p><p><strong>参照</strong>:</p><p>getwork getposi getangi getangri getscalei getdiri getefxi</p><h2 id="selpos" tabindex="-1">selpos <a class="header-anchor" href="#selpos" aria-label="Permalink to &quot;selpos {#selpos}&quot;">​</a></h2><p>移動座標をMOC情報に設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id id : オブジェクトID</p><p><strong>説明</strong>:</p><p>MOC設定命令の対象となるMOCグループをpos(座標)に設定します idは、オブジェクトIDとなります。</p><p><strong>参照</strong>:</p><p>selmoc selang selscale seldir selefx selcam selcpos selcang selcint</p><h2 id="selang" tabindex="-1">selang <a class="header-anchor" href="#selang" aria-label="Permalink to &quot;selang {#selang}&quot;">​</a></h2><p>回転角度をMOC情報に設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id id : オブジェクトID</p><p><strong>説明</strong>:</p><p>MOC設定命令の対象となるMOCグループをang(回転角度)に設定します idは、オブジェクトIDとなります。</p><p><strong>参照</strong>:</p><p>selmoc selpos selscale seldir selefx selcam selcpos selcang selcint</p><h2 id="selscale" tabindex="-1">selscale <a class="header-anchor" href="#selscale" aria-label="Permalink to &quot;selscale {#selscale}&quot;">​</a></h2><p>スケールをMOC情報に設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id id : オブジェクトID</p><p><strong>説明</strong>:</p><p>MOC設定命令の対象となるMOCグループをscale(スケール)に設定します idは、オブジェクトIDとなります。</p><p><strong>参照</strong>:</p><p>selmoc selpos selang selefx seldir selcam selcpos selcang selcint</p><h2 id="seldir" tabindex="-1">seldir <a class="header-anchor" href="#seldir" aria-label="Permalink to &quot;seldir {#seldir}&quot;">​</a></h2><p>移動量をMOC情報に設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id id : オブジェクトID</p><p><strong>説明</strong>:</p><p>MOC設定命令の対象となるMOCグループをdir(移動量)に設定します idは、オブジェクトIDとなります。</p><p><strong>参照</strong>:</p><p>selmoc selpos selang selscale selefx selcam selcpos selcang selcint</p><h2 id="selwork" tabindex="-1">selwork <a class="header-anchor" href="#selwork" aria-label="Permalink to &quot;selwork {#selwork}&quot;">​</a></h2><p>オブジェクトワークをMOC情報に設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id id : オブジェクトID</p><p><strong>説明</strong>:</p><p>MOC設定命令の対象となるMOCグループをwork(ワーク)に設定します idは、オブジェクトIDとなります。</p><p><strong>参照</strong>:</p><p>selmoc selpos selang selscale selefx selcam selcpos selcang selcint</p><h2 id="objset3" tabindex="-1">objset3 <a class="header-anchor" href="#objset3" aria-label="Permalink to &quot;objset3 {#objset3}&quot;">​</a></h2><p>MOC情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>x,y,z x : 設定する値 y : 設定する値2 z : 設定する値3</p><p><strong>説明</strong>:</p><p>MOC情報を設定します。 オフセット番号0から3つのパラメータが対象になります。</p><p><strong>参照</strong>:</p><p>objset3 objadd3 objset3r objsetf3 objaddf3</p><h2 id="objsetf3" tabindex="-1">objsetf3 <a class="header-anchor" href="#objsetf3" aria-label="Permalink to &quot;objsetf3 {#objsetf3}&quot;">​</a></h2><p>MOC情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fx,fy,fz fx : 設定する値(実数値) fy : 設定する値2(実数値) fz : 設定する値3(実数値)</p><p><strong>説明</strong>:</p><p>MOC情報を設定します。 オフセット番号0から3つのパラメータが対象になります。</p><p><strong>参照</strong>:</p><p>objset3 objadd3 objset3r objsetf3 objaddf3</p><h2 id="objadd3" tabindex="-1">objadd3 <a class="header-anchor" href="#objadd3" aria-label="Permalink to &quot;objadd3 {#objadd3}&quot;">​</a></h2><p>MOC情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>x,y,z x : 加算する値 y : 加算する値2 z : 加算する値3</p><p><strong>説明</strong>:</p><p>MOC情報に設定されている値にx,y,zを加算します。 オフセット番号0から3つのパラメータが対象になります。</p><p><strong>参照</strong>:</p><p>objset3 objadd3r objset3r objsetf3 objaddf3</p><h2 id="objaddf3" tabindex="-1">objaddf3 <a class="header-anchor" href="#objaddf3" aria-label="Permalink to &quot;objaddf3 {#objaddf3}&quot;">​</a></h2><p>MOC情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fx,fy,fz fx : 加算する値(実数値) fy : 加算する値2(実数値) fz : 加算する値3(実数値)</p><p><strong>説明</strong>:</p><p>MOC情報に設定されている値にfx,fy,fzを加算します。 オフセット番号0から3つのパラメータが対象になります。</p><p><strong>参照</strong>:</p><p>objset3 objadd3 objset3r objsetf3 objaddf3</p><h2 id="objadd3r" tabindex="-1">objadd3r <a class="header-anchor" href="#objadd3r" aria-label="Permalink to &quot;objadd3r {#objadd3r}&quot;">​</a></h2><p>MOC情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>ofs,fx,fy,fz ofs : MOCのオフセット番号 fx : 加算する値(整数角度値) fy : 加算する値2(整数角度値) fz : 加算する値3(整数角度値)</p><p><strong>説明</strong>:</p><p>MOC情報に設定されている値にfx,fy,fzを加算します。 ただし整数値(256で１回転)をラジアン単位に変換したパラメーターを加算します。 角度を指定するパラメーター以外では正常な値にならないので注意してください。</p><p><strong>参照</strong>:</p><p>objset3 objadd3 objset3r objsetf3 objaddf3</p><h2 id="objset3r" tabindex="-1">objset3r <a class="header-anchor" href="#objset3r" aria-label="Permalink to &quot;objset3r {#objset3r}&quot;">​</a></h2><p>MOC情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>x,y,z x : 設定する値 y : 設定する値2 z : 設定する値3</p><p><strong>説明</strong>:</p><p>MOC情報に角度情報を設定します。 オフセット番号0から3つのパラメータが対象になります。 整数値(256で１回転)をラジアン単位に変換してパラメーターを書き込みます。 角度を指定するパラメーター以外では正常な値にならないので注意してください。</p><p><strong>参照</strong>:</p><p>objset3 objadd3 objset3r objsetf3 objaddf3</p><h2 id="setobjmode" tabindex="-1">setobjmode <a class="header-anchor" href="#setobjmode" aria-label="Permalink to &quot;setobjmode {#setobjmode}&quot;">​</a></h2><p>オブジェクトのモード設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>ObjID,mode,sw ObjID : オブジェクトID mode : モード値 sw : 設定スイッチ</p><p><strong>説明</strong>:</p><p>指定されたオブジェクトのモードを変更します。 モード値は、regobj命令で指定するものと同様です。 swは、以下のように動作します。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : 指定したモード値を追加</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : 指定したモード値を削除</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : 指定したモード値だけを設定</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : 指定したモード値を追加</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : 指定したモード値を削除</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : 指定したモード値だけを設定</span></span></code></pre></div><p><strong>参照</strong>:</p><p>regobj setobjmodel</p><h2 id="setcoli" tabindex="-1">setcoli <a class="header-anchor" href="#setcoli" aria-label="Permalink to &quot;setcoli {#setcoli}&quot;">​</a></h2><p>オブジェクトのコリジョン設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,mygroup,enegroup id : オブジェクトID mygroup : 自分が属するグループ値 enegroup : 衝突を検出する対象となるグループ値</p><p><strong>説明</strong>:</p><p>オブジェクトに対してコリジョングループ情報を設定します。 コリジョングループ値は、1,2,4,8,16,32,64,128,256,512,1024,2048,4096,8192,16384,32768の中から1つだけを選択可能です。</p><p><strong>参照</strong>:</p><p>getcoli findobj nextobj</p><h2 id="getcoli" tabindex="-1">getcoli <a class="header-anchor" href="#getcoli" aria-label="Permalink to &quot;getcoli {#getcoli}&quot;">​</a></h2><p>オブジェクトのコリジョン判定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>val,id,distance val : 結果が代入される変数名 id : オブジェクトID distance : 衝突を検出する範囲(実数値)</p><p><strong>説明</strong>:</p><p>指定したオブジェクトが持つコリジョン情報をもとに、そのオブジェクトが衝突している別なオブジェクトのIDを調べます。 distanceは、衝突する範囲(半径)を実数値で指定します。 衝突が検出された場合は、変数にオブジェクトIDが代入されます。 何も衝突が検出されなかった場合は、-1が代入されます。</p><p>HGIMG4では、distanceにマイナス値を指定した場合、3Dモデルが持つ衝突範囲(バウンディングボックス)にdistanceを掛けた値をもとに衝突検出を行ないます。 たとえば、-1.5を指定した場合は、衝突範囲を1.5倍に拡大した状態で、衝突検出が行なわれます。また、より正確な衝突の情報を作成するために、gppcontact命令を使用することが可能です。</p><p><strong>参照</strong>:</p><p>setcoli findobj nextobj gppcontact</p><h2 id="getobjcoli" tabindex="-1">getobjcoli <a class="header-anchor" href="#getobjcoli" aria-label="Permalink to &quot;getobjcoli {#getobjcoli}&quot;">​</a></h2><p>オブジェクトのグループ取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>var,id,group var : 結果が代入される変数名 id(0) : オブジェクトID group(0) : グループID</p><p><strong>説明</strong>:</p><p>指定したオブジェクトが所属するグループ値(コリジョングループなど)を取得し、varで指定された変数に代入します。 グループ値は、以下のものになります。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">グループID   内容</span></span>
<span class="line"><span style="color:#e1e4e8;">---------------------------------------</span></span>
<span class="line"><span style="color:#e1e4e8;">  0          コリジョングループ(setcoliで設定)</span></span>
<span class="line"><span style="color:#e1e4e8;">  1          衝突対象グループ(setcoliで設定)</span></span>
<span class="line"><span style="color:#e1e4e8;">  2          レンダリンググループ(setobjrenderで設定)</span></span>
<span class="line"><span style="color:#e1e4e8;">  3          ライティンググループ(setobjrenderで設定)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">グループID   内容</span></span>
<span class="line"><span style="color:#24292e;">---------------------------------------</span></span>
<span class="line"><span style="color:#24292e;">  0          コリジョングループ(setcoliで設定)</span></span>
<span class="line"><span style="color:#24292e;">  1          衝突対象グループ(setcoliで設定)</span></span>
<span class="line"><span style="color:#24292e;">  2          レンダリンググループ(setobjrenderで設定)</span></span>
<span class="line"><span style="color:#24292e;">  3          ライティンググループ(setobjrenderで設定)</span></span></code></pre></div><p><strong>参照</strong>:</p><p>setcoli getcoli setobjrender</p><h2 id="setobjrender" tabindex="-1">setobjrender <a class="header-anchor" href="#setobjrender" aria-label="Permalink to &quot;setobjrender {#setobjrender}&quot;">​</a></h2><p>オブジェクトのレンダリンググループ設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,rendergroup,lightgroup id(0) : オブジェクトID rendergroup(1) : レンダリンググループ値 lightgroup(1) : ライティンググループ値</p><p><strong>説明</strong>:</p><p>オブジェクトに対してレンダリンググループ、ライティンググループ情報を設定します。 レンダリンググループ値は、カメラからレンダリングした際に表示のON/OFFを設定するための値です。カメラが持つレンダリンググループ値と同一である場合は、表示が有効となります。 通常は、オブジェクト、カメラともにグループ1が設定されています。特定のカメラからの表示のみ表示を無効にしたい場合などに利用できます。 ライティンググループ値は、特定のライトに対して有効/無効を切り替えるものです。ライトが持つライティンググループ値と異なる場合は、ライトが無効となります。</p><p>それぞれのグループ値は、1,2,4,8,16,32,64,128,256,512,1024,2048,4096,8192,16384,32768の中から任意のビットを組み合わせて指定可能です。</p><p><strong>参照</strong>:</p><p>setcoli getobjcoli</p><h2 id="findobj" tabindex="-1">findobj <a class="header-anchor" href="#findobj" aria-label="Permalink to &quot;findobj {#findobj}&quot;">​</a></h2><p>オブジェクト検索</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>exmode,group exmode(0) : 検索を除外するモード group(0) : 検索対象コリジョングループ値</p><p><strong>説明</strong>:</p><p>有効なオブジェクトを列挙します。 コリジョングループ値を指定した場合は、特定のコリジョングループに属するオブジェクトだけを検索します。 コリジョングループ値が0の場合は、すべてのオブジェクトが検索対象となります。 最初にfindobjを実行して、次にnextobj命令で該当するオブジェクトを検索することができます。 また、exmodeで指定したモード(regobjで指定するモード値と同じ)は検索から除外されます。</p><p><strong>参照</strong>:</p><p>setcoli nextobj</p><h2 id="nextobj" tabindex="-1">nextobj <a class="header-anchor" href="#nextobj" aria-label="Permalink to &quot;nextobj {#nextobj}&quot;">​</a></h2><p>次のオブジェクト検索</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>val val : 結果が代入される変数名</p><p><strong>説明</strong>:</p><p>findobj命令で指定された条件をもとにオブジェクトを検索します。 検索されると、変数にオブジェクトIDが代入されます。 検索対象がなくなった時には-1が代入されます。</p><p><strong>参照</strong>:</p><p>setcoli findobj</p><h2 id="setborder" tabindex="-1">setborder <a class="header-anchor" href="#setborder" aria-label="Permalink to &quot;setborder {#setborder}&quot;">​</a></h2><p>オブジェクト有効範囲設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fx,fy,fz,option ( fx,fy,fz ) : ボーダー領域の設定値(実数値) option(0) : 設定オプション(0〜2)</p><p><strong>説明</strong>:</p><p>ボーダー領域(オブジェクト有効範囲)を設定します。 optionパラメーターにより、( fx,fy,fz )に設定する内容が変わります。 optionパラメーターを省略するか、または0の場合は、 ( 0,0,0 )を中心にした、( fx,fy,fz )サイズの立方体がボーダー領域となります。 optionパラメーターが1の場合は、( fx,fy,fz )の座標を数値が小さい側のボーダー領域として設定します。 optionパラメーターが2の場合は、( fx,fy,fz )の座標を数値が大きい側のボーダー領域として設定します。</p><p><strong>参照</strong>:</p><p>regobj setobjmode</p><h2 id="selmoc" tabindex="-1">selmoc <a class="header-anchor" href="#selmoc" aria-label="Permalink to &quot;selmoc {#selmoc}&quot;">​</a></h2><p>MOC情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id, mocofs id : オブジェクトID mocofs : MOCのグループ指定</p><p><strong>説明</strong>:</p><p>MOC設定命令の対象となるMOCグループを指定します。 idは、オブジェクトIDとなります。 通常は、selpos,selang,selscale,seldir命令をお使いください。</p><p><strong>参照</strong>:</p><p>selpos selang selscale seldir selcam selcpos selcang selcint</p><h2 id="objgetfv" tabindex="-1">objgetfv <a class="header-anchor" href="#objgetfv" aria-label="Permalink to &quot;objgetfv {#objgetfv}&quot;">​</a></h2><p>MOC情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv fv = FV値が代入される変数名</p><p><strong>説明</strong>:</p><p>MOCに設定されている値を変数fvに代入します。</p><p><strong>参照</strong>:</p><p>objsetfv fvset fvadd fvsub fvmul fvdiv</p><h2 id="objsetfv" tabindex="-1">objsetfv <a class="header-anchor" href="#objsetfv" aria-label="Permalink to &quot;objsetfv {#objsetfv}&quot;">​</a></h2><p>MOC情報を設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv fv = FV値が代入されている変数名</p><p><strong>説明</strong>:</p><p>変数fvの内容をMOCに設定します。</p><p><strong>参照</strong>:</p><p>objgetfv fvset fvadd fvsub fvmul fvdiv</p><h2 id="objaddfv" tabindex="-1">objaddfv <a class="header-anchor" href="#objaddfv" aria-label="Permalink to &quot;objaddfv {#objaddfv}&quot;">​</a></h2><p>MOC情報を加算</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>fv fv = FV値が代入されている変数名</p><p><strong>説明</strong>:</p><p>変数fvの内容をMOCに加算します。</p><p><strong>参照</strong>:</p><p>objgetfv fvset fvadd fvsub fvmul fvdiv</p><h2 id="objexist" tabindex="-1">objexist <a class="header-anchor" href="#objexist" aria-label="Permalink to &quot;objexist {#objexist}&quot;">​</a></h2><p>オブジェクトIDが有効か調べる</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>p1 p1(0) : オブジェクトID</p><p><strong>説明</strong>:</p><p>p1で指定されたオブジェクトIDが有効であるか調べます。 オブジェクトIDが有効(登録済み)の場合は、システム変数statに0が代入されます。 オブジェクトIDが無効(未登録)の場合は、システム変数statに-1が代入されます。</p><p><strong>参照</strong>:</p><p>regobj delobj</p><h2 id="event_wait" tabindex="-1">event_wait <a class="header-anchor" href="#event_wait" aria-label="Permalink to &quot;event_wait {#event_wait}&quot;">​</a></h2><p>待ち時間イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,p1 id : イベントID p1(0) : 待ち時間(フレーム)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、待ち時間イベントを追加します。 待ち時間イベントは、p1で指定されたフレーム数だけ次のイベントに進むことを保留します。</p><p><strong>参照</strong>:</p><p>newevent setevent</p><h2 id="event_jump" tabindex="-1">event_jump <a class="header-anchor" href="#event_jump" aria-label="Permalink to &quot;event_jump {#event_jump}&quot;">​</a></h2><p>ジャンプイベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,p1,p2 id : イベントID p1(0) : ジャンプ先のイベント番号 p2(0) : ジャンプ無視の確率(%)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、ジャンプイベントを追加します。 ジャンプイベントは、指定されたイベント番号から実行を続けることを指示します。 イベントリストの中でのgoto命令にあたります。 p1で指定するイベント番号は、イベントに追加された順番に0,1,2…と数えたものになります。 p2で、ジャンプ無視の確率(%)を設定することができます。 0または省略された場合は、必ず(無条件)でジャンプを行ないます。 それ以外の場合は、乱数をもとに1〜100%の確率でジャンプを行ない、 ジャンプしなかった場合は次のイベントに進みます。</p><p><strong>参照</strong>:</p><p>newevent setevent</p><h2 id="event_prmset" tabindex="-1">event_prmset <a class="header-anchor" href="#event_prmset" aria-label="Permalink to &quot;event_prmset {#event_prmset}&quot;">​</a></h2><p>パラメーター設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,p1,p2 id : イベントID p1(0) : パラメーターID(PRMSET_*) p2(0) : 設定される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、パラメーター設定イベントを追加します。 パラメーター設定イベントは、p1で指定されたパラメーターIDにp2の値を設定します。 (それまでに設定されていた内容は消去されます) パラメーターIDには、以下の名前を使用することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">パラメーターID   内容</span></span>
<span class="line"><span style="color:#e1e4e8;">---------------------------------------</span></span>
<span class="line"><span style="color:#e1e4e8;">PRMSET_MODE      動作モード</span></span>
<span class="line"><span style="color:#e1e4e8;">PRMSET_FLAG      存在フラグ</span></span>
<span class="line"><span style="color:#e1e4e8;">PRMSET_SHADE     シェーディングモード</span></span>
<span class="line"><span style="color:#e1e4e8;">PRMSET_TIMER     タイマー</span></span>
<span class="line"><span style="color:#e1e4e8;">PRMSET_MYGROUP   コリジョングループ値</span></span>
<span class="line"><span style="color:#e1e4e8;">PRMSET_COLGROUP  対象グループ値</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">パラメーターID   内容</span></span>
<span class="line"><span style="color:#24292e;">---------------------------------------</span></span>
<span class="line"><span style="color:#24292e;">PRMSET_MODE      動作モード</span></span>
<span class="line"><span style="color:#24292e;">PRMSET_FLAG      存在フラグ</span></span>
<span class="line"><span style="color:#24292e;">PRMSET_SHADE     シェーディングモード</span></span>
<span class="line"><span style="color:#24292e;">PRMSET_TIMER     タイマー</span></span>
<span class="line"><span style="color:#24292e;">PRMSET_MYGROUP   コリジョングループ値</span></span>
<span class="line"><span style="color:#24292e;">PRMSET_COLGROUP  対象グループ値</span></span></code></pre></div><p><strong>参照</strong>:</p><p>event_prmon event_prmoff newevent setevent</p><h2 id="event_prmon" tabindex="-1">event_prmon <a class="header-anchor" href="#event_prmon" aria-label="Permalink to &quot;event_prmon {#event_prmon}&quot;">​</a></h2><p>パラメータービット設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,p1,p2 id : イベントID p1(0) : パラメーターID(PRMSET_*) p2(0) : 設定されるビット</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、パラメータービット設定イベントを追加します。 パラメータービット設定イベントは、p1で指定されたパラメーターIDにp2のビットを設定します。 (それまでに設定されていた内容は保持されたまま、新しい値のビットだけが有効になります) パラメーターIDの詳細については、event_prmset命令を参照してください。</p><p><strong>参照</strong>:</p><p>event_prmset event_prmoff newevent setevent</p><h2 id="event_prmoff" tabindex="-1">event_prmoff <a class="header-anchor" href="#event_prmoff" aria-label="Permalink to &quot;event_prmoff {#event_prmoff}&quot;">​</a></h2><p>パラメータービット消去イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,p1,p2 id : イベントID p1(0) : パラメーターID(PRMSET_*) p2(0) : 消去されるビット</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、パラメータービット消去イベントを追加します。 パラメータービット消去イベントは、p1で指定されたパラメーターIDから、 p2のビットだけを消去します。 パラメーターIDの詳細については、event_prmset命令を参照してください。</p><p><strong>参照</strong>:</p><p>event_prmset event_prmon newevent setevent</p><h2 id="event_setpos" tabindex="-1">event_setpos <a class="header-anchor" href="#event_setpos" aria-label="Permalink to &quot;event_setpos {#event_setpos}&quot;">​</a></h2><p>posグループ設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x1,y1,z1,x2,y2,z2 id : イベントID (x1,y1,z1) : 設定される値(下限値) (x2,y2,z2) : 設定される値(上限値)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ設定イベントを追加します。 グループ設定イベントは、オブジェクトが持っているパラメーターを設定します。 (x1,y1,z1)と(x2,y2,z2)を指定すると、それぞれの範囲内にある値を乱数で作成します。 (x2,y2,z2)を省略して、(x1,y1,z1)だけを指定した場合はそのまま値が設定されます。</p><p><strong>参照</strong>:</p><p>event_setang event_setangr event_setscale event_setdir event_setefx event_setwork newevent setevent</p><h2 id="event_setang" tabindex="-1">event_setang <a class="header-anchor" href="#event_setang" aria-label="Permalink to &quot;event_setang {#event_setang}&quot;">​</a></h2><p>angグループ設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x1,y1,z1,x2,y2,z2 id : イベントID (x1,y1,z1) : 設定される値(下限値) (x2,y2,z2) : 設定される値(上限値)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ設定イベントを追加します。 グループ設定イベントは、オブジェクトが持っているパラメーターを設定します。 (x1,y1,z1)と(x2,y2,z2)を指定すると、それぞれの範囲内にある値を乱数で作成します。 (x2,y2,z2)を省略して、(x1,y1,z1)だけを指定した場合はそのまま値が設定されます。</p><p><strong>参照</strong>:</p><p>event_setpos event_setangr event_setscale event_setdir event_setefx event_setwork newevent setevent</p><h2 id="event_setangr" tabindex="-1">event_setangr <a class="header-anchor" href="#event_setangr" aria-label="Permalink to &quot;event_setangr {#event_setangr}&quot;">​</a></h2><p>angグループ設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x1,y1,z1,x2,y2,z2 id : イベントID (x1,y1,z1) : 設定される値(下限値) (x2,y2,z2) : 設定される値(上限値)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ設定イベントを追加します。 グループ設定イベントは、オブジェクトが持っているパラメーターを設定します。 (x1,y1,z1)と(x2,y2,z2)を指定すると、それぞれの範囲内にある値を乱数で作成します。 (x2,y2,z2)を省略して、(x1,y1,z1)だけを指定した場合はそのまま値が設定されます。</p><p><strong>参照</strong>:</p><p>event_setpos event_setang event_setscale event_setdir event_setefx event_setwork newevent setevent</p><h2 id="event_setscale" tabindex="-1">event_setscale <a class="header-anchor" href="#event_setscale" aria-label="Permalink to &quot;event_setscale {#event_setscale}&quot;">​</a></h2><p>scaleグループ設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x1,y1,z1,x2,y2,z2 id : イベントID (x1,y1,z1) : 設定される値(下限値) (x2,y2,z2) : 設定される値(上限値)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ設定イベントを追加します。 グループ設定イベントは、オブジェクトが持っているパラメーターを設定します。 (x1,y1,z1)と(x2,y2,z2)を指定すると、それぞれの範囲内にある値を乱数で作成します。 (x2,y2,z2)を省略して、(x1,y1,z1)だけを指定した場合はそのまま値が設定されます。</p><p><strong>参照</strong>:</p><p>event_setpos event_setang event_setangr event_setdir event_setefx event_setwork newevent setevent</p><h2 id="event_setdir" tabindex="-1">event_setdir <a class="header-anchor" href="#event_setdir" aria-label="Permalink to &quot;event_setdir {#event_setdir}&quot;">​</a></h2><p>dirグループ設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x1,y1,z1,x2,y2,z2 id : イベントID (x1,y1,z1) : 設定される値(下限値) (x2,y2,z2) : 設定される値(上限値)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ設定イベントを追加します。 グループ設定イベントは、オブジェクトが持っているパラメーターを設定します。 (x1,y1,z1)と(x2,y2,z2)を指定すると、それぞれの範囲内にある値を乱数で作成します。 (x2,y2,z2)を省略して、(x1,y1,z1)だけを指定した場合はそのまま値が設定されます。</p><p><strong>参照</strong>:</p><p>event_setpos event_setang event_setangr event_setscale event_setefx event_setwork newevent setevent</p><h2 id="event_setwork" tabindex="-1">event_setwork <a class="header-anchor" href="#event_setwork" aria-label="Permalink to &quot;event_setwork {#event_setwork}&quot;">​</a></h2><p>workグループ設定イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x1,y1,z1,x2,y2,z2 id : イベントID (x1,y1,z1) : 設定される値(下限値) (x2,y2,z2) : 設定される値(上限値)</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ設定イベントを追加します。 グループ設定イベントは、オブジェクトが持っているパラメーターを設定します。 (x1,y1,z1)と(x2,y2,z2)を指定すると、それぞれの範囲内にある値を乱数で作成します。 (x2,y2,z2)を省略して、(x1,y1,z1)だけを指定した場合はそのまま値が設定されます。</p><p><strong>参照</strong>:</p><p>event_setpos event_setang event_setangr event_setscale event_setdir event_setefx newevent setevent</p><h2 id="event_pos" tabindex="-1">event_pos <a class="header-anchor" href="#event_pos" aria-label="Permalink to &quot;event_pos {#event_pos}&quot;">​</a></h2><p>posグループ変化イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,frame,x1,y1,z1,sw id : イベントID frame : 変化までのフレーム数 (x1,y1,z1) : 設定される値 sw(1) : 補間オプション</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ変化イベントを追加します。 グループ変化イベントは、オブジェクトが持っているパラメーターの時間による変化を設定します。 frameで指定したフレーム数が経過した時に(x1,y1,z1)の値になります。 swの補間オプションは、以下の値を指定することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre></div><p>swを省略した場合には、絶対値スプラインが設定されます。 swの値に16を加算した場合は、(x1,y1,z1)で設定される値の替わりにオブジェクトが持つworkグループの値を使用して変化を設定します。</p><p><strong>参照</strong>:</p><p>event_ang event_angr event_scale event_dir event_efx event_work newevent setevent</p><h2 id="event_ang" tabindex="-1">event_ang <a class="header-anchor" href="#event_ang" aria-label="Permalink to &quot;event_ang {#event_ang}&quot;">​</a></h2><p>angグループ変化イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,frame,x1,y1,z1,sw id : イベントID frame : 変化までのフレーム数 (x1,y1,z1) : 設定される値 sw(0) : 補間オプション</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ変化イベントを追加します。 グループ変化イベントは、オブジェクトが持っているパラメーターの時間による変化を設定します。 frameで指定したフレーム数が経過した時に(x1,y1,z1)の値になります。 swの補間オプションは、以下の値を指定することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre></div><p>swを省略した場合には、絶対値リニアが設定されます。 swの値に16を加算した場合は、(x1,y1,z1)で設定される値の替わりにオブジェクトが持つworkグループの値を使用して変化を設定します。 (角度の指定は、setang命令と同様で、回転する順番はX-&gt;Y-&gt;Zとなります。他の順番で回転させるための、event_angy、event_angz命令が用意されています。)</p><p><strong>参照</strong>:</p><p>event_pos event_angr event_scale event_dir event_efx event_work newevent setevent</p><h2 id="event_angr" tabindex="-1">event_angr <a class="header-anchor" href="#event_angr" aria-label="Permalink to &quot;event_angr {#event_angr}&quot;">​</a></h2><p>angグループ変化イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,frame,x1,y1,z1 id : イベントID frame : 変化までのフレーム数 (x1,y1,z1) : 設定される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ変化イベントを追加します。 グループ変化イベントは、オブジェクトが持っているパラメーターの時間による変化を設定します。 frameで指定したフレーム数が経過した時に(x1,y1,z1)の値になります。 swの補間オプションは、以下の値を指定することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre></div><p>swを省略した場合には、絶対値リニアが設定されます。 swの値に16を加算した場合は、(x1,y1,z1)で設定される値の替わりにオブジェクトが持つworkグループの値を使用して変化を設定します。</p><p><strong>参照</strong>:</p><p>event_pos event_ang event_scale event_dir event_efx event_work newevent setevent</p><h2 id="event_scale" tabindex="-1">event_scale <a class="header-anchor" href="#event_scale" aria-label="Permalink to &quot;event_scale {#event_scale}&quot;">​</a></h2><p>scaleグループ変化イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,frame,x1,y1,z1,sw id : イベントID frame : 変化までのフレーム数 (x1,y1,z1) : 設定される値 sw(0) : 補間オプション</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ変化イベントを追加します。 グループ変化イベントは、オブジェクトが持っているパラメーターの時間による変化を設定します。 frameで指定したフレーム数が経過した時に(x1,y1,z1)の値になります。 swの補間オプションは、以下の値を指定することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre></div><p>swを省略した場合には、絶対値リニアが設定されます。 swの値に16を加算した場合は、(x1,y1,z1)で設定される値の替わりにオブジェクトが持つworkグループの値を使用して変化を設定します。</p><p><strong>参照</strong>:</p><p>event_pos event_ang event_angr event_dir event_efx event_work newevent setevent</p><h2 id="event_dir" tabindex="-1">event_dir <a class="header-anchor" href="#event_dir" aria-label="Permalink to &quot;event_dir {#event_dir}&quot;">​</a></h2><p>dirグループ変化イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,frame,x1,y1,z1,sw id : イベントID frame : 変化までのフレーム数 (x1,y1,z1) : 設定される値 sw(0) : 補間オプション</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ変化イベントを追加します。 グループ変化イベントは、オブジェクトが持っているパラメーターの時間による変化を設定します。 frameで指定したフレーム数が経過した時に(x1,y1,z1)の値になります。 swの補間オプションは、以下の値を指定することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre></div><p>swを省略した場合には、絶対値リニアが設定されます。 swの値に16を加算した場合は、(x1,y1,z1)で設定される値の替わりにオブジェクトが持つworkグループの値を使用して変化を設定します。</p><p><strong>参照</strong>:</p><p>event_pos event_ang event_angr event_scale event_efx event_work newevent setevent</p><h2 id="event_work" tabindex="-1">event_work <a class="header-anchor" href="#event_work" aria-label="Permalink to &quot;event_work {#event_work}&quot;">​</a></h2><p>workグループ変化イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,frame,x1,y1,z1,sw id : イベントID frame : 変化までのフレーム数 (x1,y1,z1) : 設定される値 sw(0) : 補間オプション</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ変化イベントを追加します。 グループ変化イベントは、オブジェクトが持っているパラメーターの時間による変化を設定します。 frameで指定したフレーム数が経過した時に(x1,y1,z1)の値になります。 swの補間オプションは、以下の値を指定することができます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#e1e4e8;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">sw = 0 : リニア補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 1 : スプライン補間(絶対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 2 : リニア補間(相対値)</span></span>
<span class="line"><span style="color:#24292e;">	sw = 3 : スプライン補間(相対値)</span></span></code></pre></div><p>swを省略した場合には、絶対値リニアが設定されます。</p><p><strong>参照</strong>:</p><p>event_pos event_ang event_angr event_scale event_dir event_efx newevent setevent</p><h2 id="event_addpos" tabindex="-1">event_addpos <a class="header-anchor" href="#event_addpos" aria-label="Permalink to &quot;event_addpos {#event_addpos}&quot;">​</a></h2><p>posグループ加算イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : イベントID (x,y,z) : 加算される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ加算イベントを追加します。 グループ加算イベントは、オブジェクトが持っているパラメーターに(x,y,z)の値を加算します。</p><p><strong>参照</strong>:</p><p>event_addang event_addangr event_addscale event_adddir event_addefx event_addwork newevent setevent</p><h2 id="event_addang" tabindex="-1">event_addang <a class="header-anchor" href="#event_addang" aria-label="Permalink to &quot;event_addang {#event_addang}&quot;">​</a></h2><p>angグループ加算イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : イベントID (x,y,z) : 加算される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ加算イベントを追加します。 グループ加算イベントは、オブジェクトが持っているパラメーターに(x,y,z)の値を加算します。</p><p><strong>参照</strong>:</p><p>event_addpos event_addangr event_addscale event_adddir event_addefx event_addwork newevent setevent</p><h2 id="event_addangr" tabindex="-1">event_addangr <a class="header-anchor" href="#event_addangr" aria-label="Permalink to &quot;event_addangr {#event_addangr}&quot;">​</a></h2><p>angグループ加算イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : イベントID (x,y,z) : 加算される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ加算イベントを追加します。 グループ加算イベントは、オブジェクトが持っているパラメーターに(x,y,z)の値を加算します。</p><p><strong>参照</strong>:</p><p>event_addpos event_addang event_addscale event_adddir event_addefx event_addwork newevent setevent</p><h2 id="event_addscale" tabindex="-1">event_addscale <a class="header-anchor" href="#event_addscale" aria-label="Permalink to &quot;event_addscale {#event_addscale}&quot;">​</a></h2><p>scaleグループ加算イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : イベントID (x,y,z) : 加算される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ加算イベントを追加します。 グループ加算イベントは、オブジェクトが持っているパラメーターに(x,y,z)の値を加算します。</p><p><strong>参照</strong>:</p><p>event_addpos event_addang event_addangr event_adddir event_addefx event_addwork newevent setevent</p><h2 id="event_adddir" tabindex="-1">event_adddir <a class="header-anchor" href="#event_adddir" aria-label="Permalink to &quot;event_adddir {#event_adddir}&quot;">​</a></h2><p>dirグループ加算イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : イベントID (x,y,z) : 加算される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ加算イベントを追加します。 グループ加算イベントは、オブジェクトが持っているパラメーターに(x,y,z)の値を加算します。</p><p><strong>参照</strong>:</p><p>event_addpos event_addang event_addangr event_addscale event_addefx event_addwork newevent setevent</p><h2 id="event_addwork" tabindex="-1">event_addwork <a class="header-anchor" href="#event_addwork" aria-label="Permalink to &quot;event_addwork {#event_addwork}&quot;">​</a></h2><p>workグループ加算イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : イベントID (x,y,z) : 加算される値</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、グループ加算イベントを追加します。 グループ加算イベントは、オブジェクトが持っているパラメーターに(x,y,z)の値を加算します。</p><p><strong>参照</strong>:</p><p>event_addpos event_addang event_addangr event_addscale event_adddir event_addefx newevent setevent</p><h2 id="setevent" tabindex="-1">setevent <a class="header-anchor" href="#setevent" aria-label="Permalink to &quot;setevent {#setevent}&quot;">​</a></h2><p>オブジェクトにイベントを設定</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>p1,p2,p3 p1(0) : オブジェクトID p2(0) : イベントID p3(-1) : イベントスロットID</p><p><strong>説明</strong>:</p><p>p1で指定したオブジェクトにp2のイベントを適用します。 あらかじめ、決まった流れの処理(イベント)を登録したイベントリストを用意しておく必要があります。</p><p>setevent命令によって設定されるイベントは、オブジェクト１つあたり４つまで同時に適用することが可能です。 p3にイベントを設定するためのイベントスロットID(0から3まで)を指定することができます。 p3を省略するか、-1を指定した場合には0から順番に空いているイベントスロットIDが使用されます。 オブジェクトに設定されたイベントを削除する場合には、p3にイベントスロットIDを指定して、p2をマイナス値にしてください。</p><p>イベントの設定に成功した場合には、システム変数statに設定されたイベントスロットIDが代入されます。 イベントの設定に失敗すると、システム変数statに-1が代入されます。</p><p><strong>参照</strong>:</p><p>newevent</p><h2 id="delevent" tabindex="-1">delevent <a class="header-anchor" href="#delevent" aria-label="Permalink to &quot;delevent {#delevent}&quot;">​</a></h2><p>イベントリストを削除</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>p1 p1 : イベントID</p><p><strong>説明</strong>:</p><p>p1で指定したイベントリストを削除します。</p><p><strong>参照</strong>:</p><p>newevent</p><h2 id="newevent" tabindex="-1">newevent <a class="header-anchor" href="#newevent" aria-label="Permalink to &quot;newevent {#newevent}&quot;">​</a></h2><p>イベントリストを作成</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>p1 p1 : イベントIDが代入される変数名</p><p><strong>説明</strong>:</p><p>新しいイベントIDを取得し、p1で指定した変数に代入します。</p><p>新しくイベントを作成する場合には、必ずnewevent命令でイベントIDを取得しておく必要があります。 次に、「event_」で始まるイベントリスト追加命令によって多彩な動作を登録しておくことができます。 一度取得されたイベントIDは、シーンのリセット(hgreset命令)が行なわれるか、 またはdelevent命令によってイベントリストが削除されるまでは保持されます。</p><p>こうしてできたイベントは、setevent命令によっていつでもオブジェクトに対して適用することができます。</p><p><strong>参照</strong>:</p><p>delevent setevent</p><h2 id="getang" tabindex="-1">getang <a class="header-anchor" href="#getang" aria-label="Permalink to &quot;getang {#getang}&quot;">​</a></h2><p>angグループ情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 angグループ(表示角度)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、実数型の変数として設定されます。 命令の最後に「i」を付加することで、整数値として値を取得することができます。</p><p><strong>参照</strong>:</p><p>getangi getpos getangr getscale getdir getefx getwork</p><h2 id="getangr" tabindex="-1">getangr <a class="header-anchor" href="#getangr" aria-label="Permalink to &quot;getangr {#getangr}&quot;">​</a></h2><p>angグループ情報を取得</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id,x,y,z id : オブジェクトID (x,y,z) : 取得する変数</p><p><strong>説明</strong>:</p><p>オブジェクトの持つパラメーターを取得します。 angグループ(表示角度)の内容が(x,y,z)で指定された変数に代入されます。 (x,y,z)は、整数型の変数として設定されます。 角度の単位は整数で0〜255で一周する値を使用します。</p><p><strong>参照</strong>:</p><p>getpos getang getscale getdir getefx getwork</p><h2 id="event_delobj" tabindex="-1">event_delobj <a class="header-anchor" href="#event_delobj" aria-label="Permalink to &quot;event_delobj {#event_delobj}&quot;">​</a></h2><p>オブジェクト削除イベントを追加</p><p><strong>グループ</strong>:</p><p>拡張画面制御命令</p><p><strong>パラメーター</strong>:</p><p>id id : イベントID</p><p><strong>説明</strong>:</p><p>idで指定しているイベントIDに、オブジェクト削除イベントを追加します。 オブジェクト削除イベントは、現在イベントを実行しているオブジェクトそのものを削除する命令です。</p><p><strong>参照</strong>:</p><p>event_regobj newevent setevent</p>`,966),o=[a];function r(g,d,l,i,c,v){return s(),p("div",null,o)}const x=e(n,[["render",r]]);export{h as __pageData,x as default};
